repos:
-   repo: local
    hooks:
    -   id: pip-audit
        name: pip-audit
        entry: pip-audit -r
        language: system
        files: requirements.txt
-   repo: https://github.com/PyCQA/bandit
    rev: '1.7.8' 
    hooks:
    -   id: bandit
        args: ["-ll", "-x", "tests"]
